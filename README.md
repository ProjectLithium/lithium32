# Lithium32

A 32-bit kernel for LithiumOS

Install libmpfr libgmp libmpc nasm gcc g++ binutils make grub qemu

Run ```toolchain.sh``` to build toolchain, this will take some time

Run ```make``` to build project, run ```make run``` to run project on QEMU